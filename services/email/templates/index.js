const contactNotification = require('./contact-notification');
const contactConfirmation = require('./contact-confirmation');
const subscriptionWelcome = require('./subscription-welcome');
const eventRegistrationConfirmation = require('./event-registration-confirmation');

module.exports = {
  contactNotification,
  contactConfirmation,
  subscriptionWelcome,
  eventRegistrationConfirmation
};